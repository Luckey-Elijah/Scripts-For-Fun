#!/bin/bash

echo "Uncomment the line for removing or adding the dock."

# remove the dock
# sudo apt remove gnome-shell-extension-ubuntu-dock

# add the dock
sudo apt install gnome-shell-extension-ubuntu-dock
